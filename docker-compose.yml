services:
  web:
    build: .
    container_name: druid_web_app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app
    
    command: conda run --no-capture-output -n DRUID uvicorn app.backend:app --host 0.0.0.0 --port 8000
    restart: unless-stopped